# lattice-music Roadmap

## Phase 15: Extraction (2026-08-23)
- [x] Extract `vir-tui` core into a standalone repository and replace local primitives with the shared dependency.
- [x] Adopt vir-tui 2.2.0's Phase-3 primitives — `progress_box()`, `interactive_session()`, `out_note()` — deleting the hand-mirrored `_TUIPbar`/shared-screen plumbing (v4.17.0).

What's done, what's next, what's deferred. Sequenced for maximum utility as a standalone library management suite. Updated as of v4.17.0.

---

## Phase 1: Foundation & Extraction (Data Layer)
- [x] **Unified Tag Reader**: `get_all_tags` returning `TagBundle` from a single `MutagenFile()` open.
- [x] **Universal Audio Support**: Extract metadata across MP3, FLAC, OGG, Opus, M4A, WAV, WMA, AAC.
- [x] **Rating Normalization**: Convert POPM, TXXX, and Vorbis comments (0-100, 0-255 scales) into a standard 0-5 float.
- [x] **Cover Art Priority Extraction**: Extract embedded art prioritizing FLAC > Opus > M4A > MP3.
- [x] **Front Cover Preference**: Prefer ID3 APIC/FLAC type 3 over generic embedded images.
- [x] **Modern Architecture**: Migrate from monolithic script to a modular `src/lattice` package via Hatch (`pyproject.toml`).
- [x] **Configurable Layout**: Support non-standard directory structures via pattern argument instead of strict `ARTIST/ALBUM/Track.ext`.
- [x] **Fix Close button trying to change music root location** - This should just exit.

## Phase 2: Integrity & Auditing (Validation Layer)
- [x] **Parallel FLAC Verification**: Spawn parallel workers to run `flac -t` or `ffmpeg` to detect corruption.
- [x] **Unified Decode Scanner**: Run parallel FFmpeg decodes for MP3 and Opus formats to detect decode errors.
- [x] **Metadata Audit**: Report audio files missing critical tags (title, artist, track number, genre).
- [x] **Duplicate Detection**: Detect the same artist + album combination appearing across multiple directories or formats.
- [x] **Missing Art Reporting**: Distinguish between directories with absolutely no art and those with only embedded art.
- [x] **Case-Insensitive Cover Detection**: Detect `cover.jpg`, `Folder.png`, etc. without case sensitivity issues.
- [x] **WAV/WMA Integrity**: Extend the unified decode scanner to cover remaining legacy formats.
- [x] **Art Quality Audit**: Report extracted/folder covers below a resolution threshold (e.g., < 500x500).
- [x] **Bitrate Floor Audit**: Flag audio files falling below a configurable bitrate floor (e.g., < 192 kbps).
- [x] **ReplayGain Audit**: Report per-album ReplayGain coverage (missing / partial / no album gain / OK), format-aware so Opus R128 gain counts as tagged. (v4.8.0)

## Phase 3: Reporting & Generation (Output Layer)
- [x] **Library Tree Generation**: Build beautiful ASCII/Unicode trees displaying artists, albums, tracks, and ratings.
- [x] **Genre Appending**: Optional `--genres` flag to inline genre tags into the library tree.
- [x] **AI-Readable Export**: Generate a token-efficient, pipe-delimited library dump designed for LLM prompts.
- [x] **Genre Wings**: Generate separate library `.txt` files for every detected genre in the collection.
- [x] **AI Wings**: Generate separate, token-efficient AI library files for each genre (Artist | Album | Genre | Location).
- [x] **Library Statistics**: Generate reports on formats, bitrates, top artists, top genres, and rating distributions.
- [x] **Playlist Export (.m3u)**: Generate standard `.m3u` playlists from library tree filters (e.g., 5-star only, specific genre).
- [x] **Smart Playlists**: Dynamic rule-based `.m3u` generation (e.g., "rating >= 4 AND genre == 'Jazz'").
- [x] **Rating Distribution per Genre**: Cross-tabulate rating spread specific to each genre inside `--stats`.

## Phase 4: Interface & Experience (UX Layer)
- [x] **Full-Screen Curses TUI**: Arrow-key navigation, color-coded section groups, and styled Unicode box drawing.
- [x] **Immersive TUI Operations**: In-menu progress bars and integrated text pagers that never drop the user back to a raw terminal shell.
- [x] **Graceful Fallbacks**: Text-based menu and typed input for environments without `curses` support.
- [x] **Persistent Configuration**: Save library root to `~/.config/lattice/config.json` on first run to eliminate redundant prompts.
- [x] **Submenus**: Nested library tree/export modes inside the TUI.
- [x] **Standalone Binary**: Support compiling a self-contained, native binary using PyInstaller.
- [x] **CLI Flag Parity**: Extensive command-line arguments mapping 1:1 with TUI capabilities (including `--dry-run` and `--version`).
- [ ] **Progress Persistence**: Resume interrupted large-scale integrity scans (FLAC/MP3) from where they left off without restarting.
- [x] **Color Output in CLI**: Status summaries are colorized (green all-clear, yellow suspect, red corrupt). Gated on an interactive TTY: off in the TUI, off when piped or redirected, off under `NO_COLOR`, so reports and pipes stay clean. (v4.6.0)
- [x] **Multi-Root Scanning**: `--root` is repeatable; several libraries scan together in one pass and de-dupe a repeated path. Cross-library duplicate detection groups an album that lives in two libraries, with each entry prefixed by its root's basename. An optional `library_roots` array in the JSON config supplies default roots; the first-run prompt still saves only the single `library_root`. (v4.6.0)
- [x] **Configurable Path Layout**: A `layout` config key (and `--layout`) sets the pattern lattice-music uses to recover artist/album/genre from a path, so a genre-first tree (`{genre}/{artist}/{album}`) is fully supported; genre now falls back to the path like artist/album already did. Default stays `{artist}/{album}`. Pairs with the `genre_foldermap.py` companion script that builds such a tree. (v4.7.0)

## Bug sweep 2026-08-09 (v4.11.0 + companion bumps)

A second full-repo review on top of v4.10.2, every finding verified against the code before fixing and regression-tested (suite 487 to 520). Details live in `patchnotes.md` under v4.11.0, apestrip.py v1.2.0, rerate.py v1.1.0, genre_tidy.py v1.3.0, cleaner.py v1.3.5, and retag.py v1.1.4.

- [x] Package: ranked rating selection (the hash-seed defect below), hidden-directory pruning in the integrity walk, path-ordered decode reports, deterministic dominant-artist tie-break, deterministic cover pick, empty-list tag value no longer raises, `stats.py` import and dead-assignment cleanup.
- [x] Package: tag reads serial by default, with `tag_workers` / `LATTICE_TAG_WORKERS` to opt back in.
- [x] apestrip.py: hidden directories pruned, sorted walk, guarded log open.
- [x] rerate.py: dry-run reports the same read errors as apply, via a shared `_popm_changes`.
- [x] genre_tidy.py: `apply` exits nonzero on retag failures; `build`'s multi-genre count no longer includes the EXCLUDED and no-genre comment rows.
- [x] cleaner.py, retag.py: guarded log open.
- Method note worth reusing: the equivalence check built a v4.10.2 baseline tree with `git show HEAD:...` in the scratchpad, held `tags.py` identical on both sides, and diffed six modes over the real library. Beware that if the baseline tree is missing, `PYTHONPATH=<gone> python -m lattice` silently falls back to the stale `~/.local` install and the diff means nothing.

### Found bug: ratings varied between runs (2026-08-09)

- [x] **FOUND BUG: rating reads depended on the interpreter's hash seed.** `get_all_tags` picked the first rating-ish key it saw and stopped, but mutagen builds a Vorbis comment's key list with `list(set(...))`, so key order is randomized per process. Any file carrying more than one rating-ish key (`rating` beside `album rating`, `love rating`, or a private `_custom_rating`) therefore reported a different rating on different runs: **41 files in the 9,589-file real library**, silently changing `--playlist "rating >= 4"` membership and the `--stats` histogram between invocations. It also let an album-level rating outrank the track's own. **Fixed (v4.11.0):** candidates are ranked rather than first-match (`_RATING_PREFERRED` / `_rating_rank` / `_best_rating`), applied to all four container branches; `tests/test_tags.py` pins permutation-invariance and the album-rating precedence, and the fix was verified stable across eight hash seeds and byte-identical over whole-library runs.

### Performance finding: threading tag reads is a regression (2026-08-09)

- [x] **Measured, then reverted.** Concurrent tag reads were proposed as the sweep's biggest win and turned out to cost more than they saved: mutagen decodes in Python, so the GIL is held for everything but the file open. With hyperfine on the real library, serial beat concurrent 1.39x for `--library`, 1.41x for `--stats`, and 1.49x for `--auditTags` (roughly 9.5s against 15.2s of user CPU). The first A/B run appeared to show a 2x speedup purely because the second invocation had a warm page cache; wall clock on this machine swings by tens of seconds under load, so **measure user CPU and warm up first**. `_scan_album_dirs` and `generate_playlist` stay serial with the numbers recorded in a comment; `stats.py` and `audit.py` keep the pool but default to serial via `config.DEFAULT_TAG_WORKERS`. The integrity modes are unaffected and genuinely benefit, because they spawn flac/ffmpeg subprocesses that do release the GIL.
- Open question, untested: whether concurrency wins on a spinning disk or a network share. Dropping the page cache needs root, so this was never measured cold; the `tag_workers` knob exists so that case can be tried without a code change.

## Bug sweep 2026-08-07 (v4.10.2 + companion bumps)

A full-repo review (package, TUI, modes, all seven companions) by three research passes, every finding verified against the code before fixing, all fixes regression-tested (suite 462 to 487). Details live in `patchnotes.md` under v4.10.2, retag.py v1.1.3, cleaner.py v1.3.4, genre_foldermap.py v1.4.1, and replaygain.py v1.2.2.

- [x] Package: POPM canonical-byte and FMPS rating scales, multi-valued ID3 join, BitrateMode row label, division-rule validation, wings `--paths` duplicate attribution, layout-aware stats album count, root-level-file layout fallback, `relpath_under("/")`, `--quiet --verbose` bar, quiet dry-run art extraction, `~` expansion for a hand-edited single root, CLI `isdir` root validation.
- [x] cleaner.py: dry-run parity for Pass 1 survivor renames across Passes 2-4; dropped duplicates excluded from the Pass 4 preview; unparseable images can no longer win a cover collision on size.
- [x] genre_foldermap.py: cross-device refusal predicted in dry-run and checked before any mkdir; multi-disc collisions flagged once; emptied genre folders pruned after `--refile-mismatched`.
- [x] retag.py: stale ID3v1 genre byte defeats `is_noop`.
- Noted, deliberately unchanged: nested-bracket suffixes in the audit's fuzzy dedup; the non-TTY confirmation auto-skip convention; the double filesystem walk (count then scan) and the serial tag reads in `generate_playlist` (candidate consolidations, not bugs).

## Found Bugs

- [x] **FOUND BUG (2026-07-03): library submenu ran `stty sane` under the persistent curses screen.** `_library_submenu` called `_reset_terminal()` unconditionally after every selection; under the v4.10.0 session screen that re-enabled echo/canonical mode, so the next prompt echoed keystrokes over the TUI and `getch()` only saw input after Enter. `_menu_session` and `_run_with_capture` had the `_SCREEN is None` gate; the submenu path was missed in the T7 rework, and the T7 pty verification drove a main-menu flow so it never hit the submenu. Surfaced by the v4.10.1 carry-back smoke test (menu → submenu → prompt → Esc). **Fixed (v4.10.1):** at the source, matching cquarry: `utils._reset_terminal` returns early while a session screen is published (`_SHARED_SCREEN`), so every call site is safe at once; `tests/test_utils.py` pins both the guard and the post-session behavior, and the pty smoke passes with one alternate-screen enter/leave.
- [x] **FOUND BUG (2026-06-10): TUI fallback menu desync.** The no-curses typed-input menu's hand-maintained key map went stale as modes were added: "6" was labelled "Test WAV files" but dispatched Extract cover art (a library-writing mode), "7"–"9" were each shifted one mode group over, "10"–"13" were rejected as invalid, and the WAV/WMA tests, art-quality audit, bitrate audit, and ReplayGain audit were unreachable from the fallback. The curses menu was unaffected. **Fixed (v4.8.1):** the fallback listing and its key map are now both generated from the same sections the arrow-key menu renders (`_build_fallback` in `tui.py`), so they cannot drift again; `tests/test_tui.py` pins the alignment.
- [x] **FOUND BUG (2026-05-25): `cleaner.py` apostrophe normalization gap.** A fragmented album survived the initial consolidation pass: JPEGMAFIA's *I LAY DOWN MY LIFE FOR YOU (Director's Cut)* was split across two sibling folders, `I LAY DOWN MY LIFE FOR YOU (DIRECTOR's CUT)/` (12 tracks, mp3+opus) and `I LAY DOWN MY LIFE FOR YOU (DIRECTORS CUT)/` (4 unique tracks, mp3). `cleaner.py` did not merge them because its normalization folded curly to straight quotes but did not equate apostrophe-present vs apostrophe-absent: `director's cut` and `directors cut` hashed to different keys. **Fixed (v4.6.0):** `normalize_name` now strips apostrophes and collapses whitespace after the NFKC/dash/quote/case folding, so `X's` and `Xs` collapse to the same key. Confirmed on the real library (it also caught a Damian "Jr. Gong" Marley quote variant).

## Audit 2026-07-01: TUI/UX + companion scripts (v4.8.1 baseline)

Full audit of `src/lattice/tui.py` plus all seven companion scripts, run 2026-07-01 against v4.8.1. Every finding below was verified against the source; the load-bearing ones were reproduced empirically (cleaner/genre_tidy against temp trees and the fixture library; replaygain against the installed `rsgain`). All 306 tests passed before the audit; every bug lives in an untested corner, so **each fix lands with the test that would have caught it**. Line numbers are as of v4.8.1 and will drift; the anchors (function names, quoted strings) are the durable reference.

Housekeeping rules for working this list: companion-script fixes get a dated `patchnotes.md` entry with a bumped script version in its docstring header where one exists (no package version bump); `tui.py`/`utils.py` fixes are package changes and bump `config.VERSION` + `pyproject.toml` + `spec.md` §1 together. Fix order: H1–H7 first (data corruption/loss on the real library), then mediums per script, then lows/UX. Tick boxes as items ship.

### High severity

- [x] **H1 (cleaner.py): `_FEAT_RE` matches `ft` inside words, corrupting artist tags non-idempotently.** `_FEAT_RE = re.compile(r"\s*(?:feat\.?|ft\.?|featuring)\s+(.*)$", re.I)` (cleaner.py:193): the leading `\s*` is zero-width, so the `ft` at the end of "Left", "Swift", "Croft" matches. Reproduced: `canon_track_artist("Left Boy", "Left Boy")` returns `"Left Boy feat. Boy"`; a second `--normalize-tags` authority pass produces `"Left Boy feat. Boy feat. Boy"` (unbounded growth). Any merged/renamed artist folder whose name contains a mid-word `ft`/`feat` gets clean tags rewritten wrong.
  **Fix:** require a real token boundary before the feat marker: `re.compile(r"(?:^|[\s(])(?:feat\.?|ft\.?|featuring)\s+(.*)$", re.I)` or equivalently add `(?<!\w)` before the alternation. While here, close the sibling paren bug (L7 below) in the same change: when the matched region began with `(` and the guest capture ends with an unmatched `)`, strip that trailing `)` from `m.group(1)` (`canon_track_artist("A (feat. B)", "A")` currently returns `"A feat. B)"`).
  **Tests (tests/test_cleaner.py, `CanonTrackArtistTests`):** `("Left Boy", "Left Boy") == "Left Boy"`; `("Left Lane Cruiser", "Left Lane Cruiser") == "Left Lane Cruiser"`; `("A (feat. B)", "A") == "A feat. B"`; idempotency: `canon_track_artist(canon_track_artist(x, c), c) == canon_track_artist(x, c)` for a real feat case.

- [x] **H2 (cleaner.py): `.wma` missing from `AUDIO_EXT`, so a differing-size `.wma` collision is deleted.** `AUDIO_EXT` (cleaner.py:82-94) lacks `.wma` while `TAGGABLE_EXT` (cleaner.py:80) and the package's canonical `AUDIO_EXTENSIONS` (src/lattice/config.py:29) both include it. Reproduced: merging `album/01.wma` (differing size) into `Album/01.wma` unlinks the source via the DROP NON-AUDIO branch (cleaner.py:449-455) instead of keeping a `.from-fragment` copy. Direct violation of the documented "audio is never overwritten or deleted" guarantee. Same gap makes `--normalize-filenames` skip `.wma` files.
  **Fix:** add `".wma"` to `AUDIO_EXT`. Make a deliberate call on `".mp4"` (in `TAGGABLE_EXT`, not `AUDIO_EXT`; defensible to exclude since `.mp4` is ambiguous with video, but document the choice in a comment). Audit `AUDIO_EXT` against `config.AUDIO_EXTENSIONS` once and note any intentional differences inline so they can't read as accidents.
  **Tests:** a merge fixture with a differing-size `.wma` collision asserting the source survives as `01.from-fragment.wma`; a `--normalize-filenames` case renaming a `.wma` track.

- [x] **H3 (cleaner.py): dry-run misses the survivor rename because the collision check ignores virtual removals.** `_normalize_folder_name` (cleaner.py:475) gates on `dst.exists()`; in dry-run the merged-away source folder still exists on disk, so the survivor rename in the flagship scenario (canonical `Drive‐By Truckers` U+2010 merging source `Drive-By Truckers`) logs `RETAIN NAME (normalized target exists)` in preview while apply performs the rename. Reproduced: dry stats `{moves:1, rmdirs:1}` vs apply `{moves:1, renamed:1, rmdirs:1}`. Knock-on: the missed rename never records the tag-authority target, so a `--normalize-tags` preview omits restamps apply performs.
  **Fix:** add a helper (e.g. `def _gone(run, p): return p in run.removed`) and change the guard in `_normalize_folder_name` and `_normalize_file_name` (cleaner.py:540) to `if dst.exists() and dst != folder and dst not in run.removed:`. The dry-run `_rename` path must also add the source path to `run.removed` (it becomes nonexistent under the new name) if it doesn't already.
  **Tests:** dry-vs-apply stats equality on the existing `SurvivorRenameTests` fixture (run the same tree twice, once dry, once apply, compare `renamed` and the tag-target set).

- [x] **H4 (genre_foldermap.py): multi-disc album subfolders misclassified as "organized", poisoning the genre vocabulary.** `classify` (genre_foldermap.py:133-134) calls any depth-3 audio dir organized, and lattice's scanner (`library._scan_album_dirs`) emits one record per audio-bearing directory at any depth. In a *flat* library, `Artist/Album/CD1` yields a depth-3 record, so `"Artist"` enters `allowed_genres` (genre_foldermap.py:196), gating turns on, and every ordinary stray is flagged `UNKNOWN GENRE`: one CD1 folder breaks the advertised flat-to-genre conversion. The disc album itself is never filed (perpetual `NOTE ... left in place`). The same failure routes through the staging inbox: `Unfiltered/Artist/Album/CD1` strips to depth 3, reads as organized, sticks in the inbox forever, and the artist name in the vocabulary can even let an unrelated stray tagged with that artist-name-as-genre pass the gate. Corollary in an *organized* library: legit `Genre/Artist/Album/CD1` dirs are depth 4 and produce perpetual `TOO DEEP` noise.
  **Fix:** teach classify about disc folders. (1) Add `DISC_DIR_RE = re.compile(r"(?i)^(?:cd|disc|disk|dvd|side|vinyl)[\s._-]*\d+$")` near the other constants. (2) In `classify`: if `len(parts) == 3` and `DISC_DIR_RE.match(parts[2])`, return `("album", parts[0], parts[1])` with the move path being the *parent* album dir (`root/parts[0]/parts[1]`), so the album moves as a unit with its discs inside; if `len(parts) == 4` and `DISC_DIR_RE.match(parts[3])`, return `("organized", parts[0], parts[1], parts[2])` instead of toodeep. (3) In `build_plan`, dedupe: CD1 and CD2 both resolve to the same album move; skip when `seen_dst.get(dst) == src` instead of logging a COLLISION. Note the classified path and the record path now differ for disc records; compute genre/vocabulary from the classified result, not the raw record. (4) Vocabulary hygiene: only true depth-3 `organized` records (not disc-derived depth-4 ones, which are already correct) contribute `info[1]` to `allowed_genres`; with the step-2 change this falls out naturally, but assert it in a test. (5) Staging leftovers: after the strip, a stripped path at depth ≥3 that is *not* a disc dir can never legitimately be "organized" relative to the inbox; classify needs to know the strip happened (pass a `staged: bool` derived from the prefix match) and return a new `("staged-toodeep", depth)` kind that `build_plan` reports as an issue (`STAGED TOO DEEP (left in inbox)`), never as organized.
  **Tests (tests/test_genre_foldermap.py):** flat library containing `Artist/Album/CD1+CD2` converts fully (gate stays off, album moves as one unit, one manifest row); organized library with `Genre/Artist/Album/CD1` produces no TOO DEEP issue and no move; `Unfiltered/Artist/Album/CD1` files the album into the taxonomy; `Unfiltered/A/B/C/weird` (non-disc) is flagged, not treated as organized; vocabulary excludes the flat-library artist name.

- [x] **H5 (genre_tidy.py + retag.py): a slash canonical can never read back compliant, causing a permanent retag loop.** `retag_argv` (genre_tidy.py:133-142) splits `"Emo / Orgcore"` on `/` into two genre args; retag writes them as a multi-value tag; the read-back never equals the map. Reproduced both ways: FLAC gets two `genre` fields and lattice's `TagBundle` reads only the first (`tags.py` `_first_text`), so compliance compares `"emo"` vs `"emo / orgcore"`; MP3's v2.3 save slash-joins `text=["Emo","Orgcore"]` into `"Emo/Orgcore"` (no spaces), which normalizes differently from `"emo / orgcore"`. Every `apply` re-runs retag on the same albums forever: files rewritten, APEv2 stripped, mtimes churned, dry-run never converges. Zero live blast radius today (`artist_genre_defaults.tsv` has slashes only in artist names), but the split is an advertised, tested feature (`test_slash_canonical_splits_into_separate_args`).
  **Fix (recommended):** retire the split. In `retag_argv`, pass the canonical verbatim as a single genre argument, so retag writes one literal genre string `"Emo / Orgcore"` in every container and the round-trip through `norm()` is exact. Update the `retag_argv` docstring (the "'/'-joined canonical is split" sentence) and replace `test_slash_canonical_splits_into_separate_args` with a test asserting the single-arg form. This treats a slash genre as one genre value, which is what DeaDBeeF/foobar display anyway and what the map's compliance check can actually verify.
  **Alternative (only if true multi-genre tags are wanted):** keep the split but (a) extend lattice's `TagBundle` genre read to join multiple Vorbis genre fields with `" / "`, (b) make retag's MP3 path write one pre-joined TCON string instead of a multi-value list, and (c) make `genre_tidy.norm()` normalize separator spacing (`"a/b"` == `"a / b"`). Strictly more moving parts across two codebases; the recommendation stands unless multi-value genre tags become a real requirement.
  **Tests (tests/test_genre_tidy.py):** end-to-end convergence: build a fixture album, apply a slash canonical, re-run compliance on the re-read tags and assert compliant (i.e. a second `apply` plans zero retags) for both a FLAC and an MP3 file.

- [x] **H6 (tui.py + utils.py): mode execution has no exception boundary and the curses lifecycle can strand the terminal.** Three legs, one cluster. (a) `_run_with_capture` (tui.py:668-670) calls `func(...)` bare and nothing upstream catches non-`KeyboardInterrupt` exceptions, so a mode error (e.g. unwritable output path raising `PermissionError` after a completed scan) escapes as a traceback, loses the results, and exits with the terminal stuck in curses mode because `_TUIPbar.draw` did `curses.initscr()` (utils.py:281) and nothing ever calls `endwin()` on this path. (b) `interactive_menu` sets `utils.IN_TUI = True` unconditionally (tui.py:798) even when `_USE_CURSES` is False (tui.py:65), so the plain-text fallback session hands progress to `_TUIPbar`; if the curses *module* is missing, the `import curses` at utils.py:276 sits before the `try` and raises `ModuleNotFoundError`; if curses exists but stdin isn't a tty, `initscr()` hijacks the screen mid-fallback-session and is never torn down. (c) Ctrl-C in non-integrity modes (audit/stats/playlists don't catch `KeyboardInterrupt`) propagates to `cli.py:381` with no terminal restore.
  **Fix:** (1) In `_run_with_capture`, wrap the `func(...)` call: `except KeyboardInterrupt` pages "[Cancelled]" plus whatever output was captured; `except Exception` captures `traceback.format_exc()` and pages it under an `[Error]` heading; in a `finally`, when `_USE_CURSES`, call `curses.endwin()` guarded by `if not curses.isendwin():` inside its own try/except, then `_reset_terminal()`. (2) Change tui.py:798 to `utils.IN_TUI = _USE_CURSES` so the fallback session never selects `_TUIPbar`; additionally move the `import curses` in `_TUIPbar.draw` inside the `try` (belt and suspenders). (3) Give `_TUIPbar.close()` a real body: `endwin()` behind the same `isendwin()` guard, so well-behaved modes restore the screen as soon as their bar closes. (4) While in utils.py, fix the factually wrong comment at utils.py:279-281 (`curses.wrapper` has already torn the screen down by the time a mode runs; `initscr()` here starts a fresh screen, it does not resume one) and replace the `box_w = 46` magic number and raw `color_pair(1)/(3)` with constants shared with tui.py (`_TUI_BOX_W`, the color-pair ids) imported or mirrored deliberately with a cross-reference comment.
  **Tests (tests/test_tui.py):** `_run_with_capture` with a raising func pages the traceback instead of propagating (monkeypatch `_tui_page` to record); with a `KeyboardInterrupt`-raising func it pages a cancel notice; `utils.IN_TUI` False path: with `IN_TUI` True but a monkeypatched unusable curses, `_make_pbar` returns `_FallbackProgress` (after fix 2 this becomes: fallback sessions never set `IN_TUI`).

- [x] **H7 (tui.py): curses init failure in `_tui_select` is indistinguishable from Quit; TUI silently exits 0 on capability-poor terminals.** `_run` calls `curses.curs_set(0)` (tui.py:244) and `_init_tui_colors` (tui.py:243) which raise `curses.error` on terminals without those capabilities (`TERM=vt100`, dumb terminals); the outer `except curses.error: return None` (tui.py:260-263) is read at tui.py:815 as "user chose Quit". Net: on such a terminal the first-run prompt works (its widget degrades gracefully), then the menu "opens" and the program instantly exits 0 with no message, even though the `_box_menu` text fallback exists and works.
  **Fix:** (1) Make the cosmetic calls non-fatal: wrap `curses.curs_set(0)` and the color init in individual `try/except curses.error: pass` (a monochrome, visible-cursor TUI beats a dead one). (2) Distinguish real init failure from Quit: catch `curses.error` from `curses.wrapper` itself, log nothing but flip the module-level `_USE_CURSES` to False and return a sentinel (e.g. the string `"fallback"`) that the caller loops on, so the very next iteration renders the text fallback menu. `_tui_prompt_str`, `_tui_pause`, `_tui_page` already degrade; `_tui_select` is the one widget mapping failure to a user action.
  **Tests:** monkeypatch `curses.wrapper` to raise `curses.error` and assert `interactive_menu` falls back to the text menu instead of returning 0; assert `curs_set` raising inside `_run` does not abort selection.

### Medium severity

- [x] **M1 (apestrip.py): `--keep-metadata` on a v1-only MP3 clobbers the ID3v1 tag.** `ID3(path)` raises `ID3NoHeaderError` for a file with only ID3v1 (mutagen's `ID3` never reads v1), so `id3 = ID3()` is empty (apestrip.py:485-487 and the repair twin at :366-368); every APE field then looks sole-source, and `id3.save(path, v2_version=3, v1=2)` (apestrip.py:515, :403) *rebuilds* ID3v1 from the sparse v2 frames, blanking whatever title/artist/album the old ID3v1 held. Exactly the old rips most likely to carry APE tags. Side effect of the same root cause: the redundancy check never sees v1 values.
  **Fix:** when the `ID3NoHeaderError` fallback fires, seed the fresh `ID3()` from the file's ID3v1 block before planning migrations: read the last 128 bytes, and if they start with `TAG`, parse via `mutagen.id3.ParseID3v1` and `id3.add()` each returned frame. Do this in both the normal path and the repair path (factor a small `_id3_for(path)` helper used by both). The redundancy check then sees the v1 values too, and `save(v1=2)` regenerates ID3v1 faithfully. **Note (shipped 2026-07-01, v1.1.1):** mutagen 1.46+ loads ID3v1 into `ID3()` itself, so on the installed 1.47 the bug never fired; `_id3_for` covers older mutagen and the test pins the behavior either way.
  **Test:** fixture MP3 with ID3v1 (title/artist set) + APEv2 carrying one TXXX-bound field, run `--keep-metadata`, assert ID3v1 title/artist survive and the APE field migrated.

- [x] **M2 (apestrip.py): multi-value APE text items flattened to a NUL-embedded string; values after the first are effectively lost.** `_ape_text` (apestrip.py:160-167) does `str(value)`; mutagen's `APETextValue.__str__` joins values with `\x00` (the raw-repair `_RawAPEValue.__str__` at :301-302 preserves NULs the same way). Saved as v2.3 UTF-16, the embedded NUL terminates the string, so foobar2000/DeaDBeeF show only the first value. Under a flag whose contract is "nothing is lost".
  **Fix:** have `_ape_text` return `str(value).split("\x00")` (a list; single-value items become a one-element list), and pass that list straight to `text=` in `apply_migration` for both native frames and TXXX (mutagen text frames accept lists). Update `migration_label` to render lists readably (`" / ".join`).
  **Test:** APE `Artist` with two values migrates to a TIT-family frame whose `.text` has both entries.

- [x] **M3 (apestrip.py): repair path drops file permissions to 0600.** `mkstemp` (apestrip.py:396) creates the temp file mode 0600 and `os.replace` (apestrip.py:409) installs it verbatim; every `--repair-malformed` repair silently strips group/other access, which breaks shared-mount libraries.
  **Fix:** before `os.replace`, copy the original's mode: `os.chmod(tmp, os.stat(path).st_mode & 0o7777)` (or `shutil.copymode(path, tmp)`).
  **Test:** chmod a fixture to 0644, repair, assert `stat.S_IMODE` still 0644.

- [x] **M4 (apestrip.py): repair-path I/O errors abort the whole run.** `open(path, "rb")` (apestrip.py:354) and `mkstemp` (apestrip.py:396) sit outside any try; one unreadable file or read-only directory raises `OSError` up through `_handle_malformed` → `process_file` → the unguarded loop in `main` (apestrip.py:658), killing a library-wide run mid-write-pass. The normal path wraps its mutations (:507-523); the repair path is asymmetric.
  **Fix:** wrap the raw-read/repair body in `try/except OSError as e: return FileResult(path, had_ape=True, error=str(e))` so the loop's existing error accounting handles it.
  **Test:** chmod-0 fixture inside a two-file run; assert the run completes and the summary counts one error.

- [x] **M5 (apestrip.py): repair excision treats everything between the APE footer and a strictly-terminal ID3v1 as junk.** `_parse_raw_ape` (apestrip.py:324, :342, :394) computes `id3v1_start = len(data) - 128` only when the *last* 128 bytes start with `TAG`, else `len(data)`, and excises `data[header_start:excise_end]`. Two traced losses (repair mode only): a Lyrics3 block sitting between APE and ID3v1 is deleted as junk; an ID3v1 followed by any trailing byte (broken downloader padding) is no longer end-anchored, so the excision deletes the ID3v1 itself. The structural proof passes and verification passes in both cases.
  **Fix:** tighten the excision to "the APE block exactly, plus only *recognized* trailing structures". After locating the APE block, examine the bytes between its end and EOF: allow (and preserve) a Lyrics3v2 block (`LYRICSBEGIN`...`LYRICS200`) and an ID3v1 (`TAG` at 128 from the end of recognized content); if any unrecognized bytes remain between the APE end and the recognized trailer chain, refuse the repair and report `unrecognized trailing data (not repaired)` instead of cutting. The excision then removes `[header_start, ape_end)` only, leaving trailers byte-for-byte.
  **Tests:** `audio|APE|Lyrics3|ID3v1` fixture repairs with Lyrics3 intact; `audio|APE|ID3v1|1 junk byte` is refused (reported, file untouched); the existing clean layouts still repair.

- [x] **M6 (apestrip.py): log asserts "stripped APEv2 tag" for files it did not touch.** The execution loop's else-branch (apestrip.py:679-682) prints the stripped line without checking `r.stripped`; a file whose APE tag vanished between the planning and write passes gets a false audit-log entry.
  **Fix:** gate the line on `r.stripped`; emit `no APEv2 tag at write time (skipped)` otherwise. While in `main`, consider returning nonzero when `errors > 0` (L-tier item A6 below) in the same patch.
  **Test:** monkeypatch/process a file whose second `process_file` sees no APE; assert log line.

- [x] **M7 (replaygain.py): custom mode fails an entire album when it contains one rsgain-unsupported file.** `scan_album` custom branch (replaygain.py:177) passes every `AUDIO_EXTENSIONS` file; verified live, `rsgain custom` rejects raw ADTS `.aac` with "File list is not valid" (rc=1) and scans *nothing*, so a 12-FLAC album with one `.aac` bonus track errors wholesale under `--target-lufs` while succeeding in default easy mode (which silently skips unsupported files).
  **Fix:** add `RSGAIN_SUPPORTED = {".mp3", ".flac", ".ogg", ".opus", ".m4a", ".mp4", ".wma", ".wav", ".aiff", ".ape", ".wv", ".mpc"}` (verify against `rsgain --help`/docs for the installed version before committing the set); in the custom branch, filter the file list to it, log any skipped files per album (`SKIP (rsgain-unsupported): ...`), and skip the album with a note if nothing remains. Add a docstring sentence about the easy/custom asymmetry.
  **Test:** mocked-run test asserting the built argv excludes a planted `.aac` and that the skip is logged.

- [x] **M8 (replaygain.py): dry-run ignores `--skip-tagged`.** The dry-run branch (replaygain.py:293-306) prints "Would scan {len(worklist)}" over the *unfiltered* worklist; the apply path filters to `to_scan` (replaygain.py:308). `--dry-run --skip-tagged` on a mostly-tagged library predicts 500 albums when apply scans 20.
  **Fix:** compute `to_scan` once, before the dry/apply split; dry-run prints per-album coverage for everything (that part is the feature) but summarizes "Would scan X of Y album(s) (Z skipped as fully tagged)" using the filtered count, and marks skipped rows in the listing.
  **Test:** dry-run with `--skip-tagged` over a half-tagged fixture asserts the "Would scan" count equals apply's `to_scan` length.

- [x] **M9 (replaygain.py): `rsgain easy` recurses, so nested album folders are rescanned and `--skip-tagged` is bypassed for them.** Verified from rsgain's help: easy mode "recursively scans a directory". `find_album_dirs` (replaygain.py:71-80) lists every audio-bearing dir independently and `scan_album` passes each dirpath, so `Album/` (loose bonus track) + `Album/CD1/` means CD1 is scanned twice, and with `--skip-tagged` a fully-tagged CD1 dropped from `to_scan` is rewritten anyway when its parent scans. Tag values come out identical (rsgain treats each dir as its own album), so the damage is redundant writes, a broken skip promise, and read-back covering only the parent's direct files. Flat `Artist/Album` leaves never hit this.
  **Fix (minimal, safe):** detect the nesting in `find_album_dirs` (an album dir with an audio-bearing descendant dir); exclude such parents from the scan list and report them (`NESTED ALBUM (skipped): Album/ has audio in subfolders; scan the subfolders or flatten`). **Fuller option, decision Brandon's:** scan such parents' direct files via `rsgain custom -a -s d <files>` so the loose tracks still get album gain as their own unit; keep the minimal skip until that's wanted. **Shipped (2026-07-01, v1.2.0): the minimal skip** (`find_nested_parents`, easy mode only; custom mode passes explicit direct files and is immune). The fuller option remains open.
  **Test:** fixture with `Album/track.mp3` + `Album/CD1/track.mp3`; assert CD1 appears once in the plan and the parent is reported, not scanned.

- [x] **M10 (rerate.py): `tags.save` outside the try; one bad file crashes the whole run and the summary is never written.** `rerate_file` (rerate.py:68-84) guards only `ID3(filepath)`; `tags.save(...)` at :83 can raise (read-only file, disk full), propagating out of a function whose docstring says "Never raises", killing `main`'s walk (rerate.py:141) with no log line for the file and no SUMMARY block.
  **Fix:** move the save inside the try (or add a second try around it), return a per-file error result, count errors in `main`, log an `ERR path: reason` line, include the count in SUMMARY, and exit nonzero when `errors > 0`.
  **Test:** read-only fixture file with byte 127 in a two-file run; assert the run completes, the other file is rerated, and SUMMARY reports 1 error.

- [x] **M11 (rerate.py): no POPM email filter; any writer's 127/254 bytes get remapped.** Lines 76-80 iterate `tags.getall("POPM")` and remap on byte value alone; MusicBee is spared only because its bytes differ. A tagger storing a 0-255 percentage under its own email (49.8% → 127) gets silently shifted.
  **Fix (decision needed):** either gate the remap on a known-writer email set (scan the real library once to enumerate the POPM emails present; DeaDBeeF/foobar/WMP values) with an `--any-email` escape hatch, or keep byte-only matching and state the assumption prominently in the docstring ("safe only in a DeaDBeeF/foobar-only library"). The map itself stays as-is. Record the choice here when made. **Choice (2026-07-01, v1.0.1): doc-only.** Byte-only matching kept; the docstring states the assumption and points at the dry-run output (which prints each frame's email). Revisit with an email gate only if a foreign tagger shows up in a dry run.
  **Test:** two POPM frames, one matching email one not, assert only the gated one is rewritten (or, if doc-only, add the docstring sentence and skip the code change).

- [x] **M12 (genre_foldermap.py): sidecar bookkeeping runs even when the album move was rejected.** `add()` (genre_foldermap.py:171-182) returns without appending on `COLLISION`/`DEST EXISTS`, but the caller (genre_foldermap.py:242-244) unconditionally records `path.parent` in `source_artist_dirs`/`album_artist_genres`. Scenario: album skipped with `DEST EXISTS`; the sidecar pass still moves `Artist/cover.jpg` to the target genre, stranding the un-moved album without its artist-level artwork.
  **Fix:** make `add()` return `bool` (True when a Move was appended); gate the two bookkeeping lines (and the loose-dir equivalents at :251-252 for the per-file adds: count successes, only register the dir if at least one file move was planned) on it.
  **Test:** fixture where the album's dst already exists; assert the sidecar stays put and `source_artist_dirs` excludes the artist dir.

- [x] **M13 (genre_foldermap.py): `revert` bypasses the Runner, so reverts have no log, no manifest, and a divergent dry-run.** `cmd_revert`-equivalent code (genre_foldermap.py:439-456) constructs a `Runner` with a `.revert.tsv` manifest path but never enters the context manager (`__enter__` never runs, `self.mf` stays None) and calls `shutil.move` directly instead of `do_move`, so a revert (which moves as much as an apply) writes no audit trail and no replay file; it also reaches into `runner._emit`/`runner.stats` (private API). Separately, dry-run revert never feeds `runner.removed`, so `prune_up` reads unchanged disk and always predicts `pruned=0` while a real revert prunes the emptied `Genre/Artist` and `Genre` dirs.
  **Fix:** wrap the revert loop in `with Runner(...) as runner:`; route each restore through `do_move(dst→src)` with kind `"revert"` so logging, manifest (the `.revert.tsv` becomes the replay file it was named for), stats, and the dry-run `removed` bookkeeping all come for free; in the dry-run branch add the vacated destination (`dstp`) to `runner.removed` so `prune_up` predicts the prunes apply will perform. Drop the `_emit` calls in favor of the public flow.
  **Tests:** revert writes a `.revert.tsv` and log lines; dry-run revert over an applied fixture predicts the same `pruned` count a real revert produces.

- [x] **M14 (genre_tidy.py): artists whose name starts with `#` can never be mapped and re-append on every rebuild.** `build_rows` (genre_tidy.py:188) emits `#1 Dad\tIndie Rock` as a data row; `parse_map` (genre_tidy.py:112) discards any line whose lstrip starts with `#` as a comment. Verified: the round-trip loses the entry; `apply` logs "not in map" forever, and `cmd_build`'s merge (genre_tidy.py:257-258) appends a duplicate row every rebuild.
  **Fix:** make the comment convention unambiguous: `parse_map` treats a line as a comment only when it matches `^\s*#(?:\s|-|$)` (space, dash, or end after the `#`), and `build_rows` guarantees its generated comments always start `# ` or `# ---` (they already do; add a test pinning it). `#1 Dad` then survives as data. Document the rule in the map-format comment block.
  **Tests:** `parse_map(build_rows(...))` round-trips a `#1 Dad` artist; generated comment lines still parse as comments; rebuild with no library change reports no new artists (also M15).

- [x] **M15 (genre_tidy.py): EXCLUDED (VA) artists re-append on every rebuild; the "No new artists" path is unreachable.** EXCLUDED artists get only a comment line (genre_tidy.py:176-181), so `parse_map` never yields their key and `cmd_build`'s `new = {k ... if k not in present}` (genre_tidy.py:258) always contains `various artists` on any library with one compilation: every `build` appends a fresh `# --- added <date> ---` marker plus a duplicate EXCLUDED comment, accumulating noise indefinitely.
  **Fix:** filter `EXCLUDED_ARTISTS` keys out of the `new` diff in `cmd_build` (they intentionally have no data row, so "present in map" can never be true for them); alternatively track flagged keys by scanning existing `# EXCLUDED:` comments, but the simple filter is enough since the exclusion set is static.
  **Test:** library with a Various Artists comp: first build writes the EXCLUDED comment once; second build reports "No new artists" and appends nothing.

- [x] **M16 (genre_tidy.py + retag.py): per-file retag failures are invisible to `apply`.** `retag.apply_genres` prints `[!] Failed...` to stdout (retag.py:104) and returns False, but `retag.main` returns 0 unconditionally (retag.py:172); `cmd_apply` counts errors only on `returncode != 0` (genre_tidy.py:341), so an album where every file failed to write is counted in `stats["retagged"]` with `errors` at 0. For a destructive tool the failure reporting is load-bearing.
  **Fix:** in retag.py, route failure messages to stderr and make `main` return 1 when any file failed (0 only on full success; document "no valid audio files" as also nonzero or as its own code, see M17). In genre_tidy's `cmd_apply`, on nonzero exit log an `ERR` line with the captured stderr and increment `errors` instead of `retagged`. Check for other retag callers before changing the exit contract (none known besides genre_tidy and manual use; note it in patchnotes).
  **Tests:** retag main returns nonzero when a file fails (read-only fixture); `cmd_apply` counts it as an error, not a retag.

- [x] **M17 (genre_tidy.py): albums in formats lattice scans but retag can't write are perpetual no-op "retags".** lattice-music's `AUDIO_EXTENSIONS` includes `.wav`/`.aac`; retag's writable set (retag.py:32) excludes them by design. A `.wav`-only album with a stray/missing genre is never compliant (`is_compliant`, genre_tidy.py:130: empty is never compliant), so every `apply` invokes retag, which skips every file, exits 0, and is counted retagged; nothing converges and nothing says "unwritable format". In mixed albums the unwritable files skip with no message at all.
  **Fix:** in `cmd_apply`, before invoking retag, check the album's extensions against retag's writable set (import or mirror `retag.SUPPORTED`/equivalent constant; prefer importing to avoid drift): if no file is writable, log `UNSUPPORTED FORMAT (skipped): <album> (.wav)` once, count under a new `stats["unsupported"]`, and don't invoke retag. In retag.py, print a per-file `skip (unsupported): name` line even when other files updated, so mixed albums are honest. Update `is_compliant`'s docstring (its "apply fills it with canonical" promise is unkeepable for these).
  **Tests:** `.wav`-only album is skipped-with-reason and stats converge on re-run; mixed album logs the per-file skip.

- [x] **M18 (cleaner.py): multi-valued tags collapsed to their first value whenever the first value needs folding.** The tag pass reads `values[0]`/`frame.text[0]` only and writes back `[val]` singletons (cleaner.py:634, :651-653, and the apply branches at :639, :657-659, :674-676, :692-694). Reproduced on FLAC: `artist=["Artist A’s Band", "Artist B"]` folds to `["Artist A's Band"]`; the co-artist is gone. Multi-valued ARTIST is idiomatic Vorbis tagging.
  **Fix:** for the *fold-only* path, fold every value (`[tag_fold(v) for v in values]`, write back the full list, changed iff any element changed). For the *authority restamp* path (artist under a merged/renamed folder), collapsing to the single canonical value is the intended semantic; keep it, but state that in a comment. Apply the same list-handling to title/album folds in all four format branches.
  **Test:** FLAC with two artist values, one needing a curly-quote fold: both survive, first is folded; restamp case still collapses to authority.

- [x] **M19 (cleaner.py): dry-run models removals but not creations; several preview divergences.** The dry-run machinery tracks `run.removed` but nothing records what apply *would create*, so later collision checks read stale disk (cleaner.py:330-334, :388, :410, :476). Reproduced: (a) three-way merge where both sources carry the same extra track: dry logs two plain `MV` (`moves:2`), apply logs `MV` + `AUDIO COLLISION` (`moves:1, collisions_kept:1`); (b) two siblings `A‐B` (U+2010) and `A‑B` (U+2011) both rendering to `A-B` under `--normalize-names`: dry `renamed:2`, apply `renamed:1` + `RETAIN NAME`. Related second-order gap: Pass 2 and Pass 3 walks (cleaner.py:894-897, :559) iterate the real filesystem without filtering `run.removed`, so dry-run scans folders apply would have deleted and misses groups apply would create.
  **Fix:** add `run.created: set[Path]` mirroring `removed`; every dry-run `_move`/`_rename`/collision-keep records its destination there; introduce one existence helper (`_fs_exists(run, p) = (p.exists() and p not in run.removed) or p in run.created`) and use it at every `.exists()` that gates a merge/rename/collision decision (:330-334, :388, the `.from-fragment` while-loop at :410-414, cover collision at :441-448, both rename guards). For the walk gap: Pass 2/3 iterdir loops skip paths in `run.removed` (full modeling of virtually-created folder contents is out of scope; note the residual limitation in the module docstring).
  **Tests:** dry-vs-apply stats equality for the three-way merge and the same-render sibling fixtures; a Pass-2 dry run doesn't report groups inside a virtually-merged-away folder.

- [x] **M20 (cleaner.py): module docstring carries stale v1.2.0 tag-pass text.** Two passes are both numbered "4." (cleaner.py:29-40) and the second claims the tag pass is "MP3-only (same scope as apestrip.py / rerate.py)" and merged-folders-scoped, contradicting shipped v1.3.0 behavior (library-wide, all formats via `normalize_file_tags`). The mojibake/naming-authority example inside it is worth keeping.
  **Fix:** delete the stale duplicate block, renumber, fold the example into the surviving pass-4 description. Pure doc change; also correct the incomplete comment at cleaner.py:489 ("Pass 3"; the hook also fires for Pass 1/2 survivor renames).

### Low severity / polish (grouped per file; one checkbox per file, tick when the batch ships)

- [x] **L-apestrip batch (shipped 2026-07-02, v1.1.2; A6 had already landed with M6).** (A1) Binary non-cover APE items become *empty* TXXX frames: `_ape_text` returns `""` for kind==1 and `classify_ape_field` routes unknown keys to TXXX (apestrip.py:160-167, :247); fix: report-and-skip binary items instead of writing junk, and correct the "no datum is ever dropped" comment at :95-97. (A2) External-locator covers (kind 2) write `APIC(data=b"")` (:226-229, :243-245); skip and report. (A3) COMM redundancy check `k.startswith("COMM")` (:195) counts `COMM:iTunNORM:eng` as "has a comment", dropping real APE Comments; compare only unqualified/desc-empty COMM frames. (A4) `_img_mime` (:170-175) defaults unknown image formats to `image/jpeg`; return None and skip with a report instead of mislabeling. (A5) Docstring at :16-18 and `--keep-metadata` help promise value-level preservation but `id3_has_equivalent` (:185-207) is presence-only; reword docs ("fields ID3 already has are considered authoritative and not overwritten"), no code change. (A6) `main` exits 0 despite per-file errors (:683-691); return 1 when `errors > 0` (align with M16's retag convention). (A7) fsync hole: when migrations exist, `id3.save(tmp)` runs *after* the flush+fsync (:398-403); re-flush and fsync after the save, before `os.replace`. (A8) Garbled `_RawAPEValue` docstring (:292-295), narration comment `# Verify the APE tag is gone.` (:516), mid-file `AUDIO_EXT` constant (:527), and the dishonest `ape: APEv2` annotation on `plan_file` (:432, also called with `dict[str, _RawAPEValue]`); tidy all four. (A9) Raw item parse has no `p + vlen <= footer` bound (:332-341); clamp and treat overflow as malformed-item (skip its migration).

- [x] **L-replaygain batch (shipped 2026-07-02, v1.2.1).** (R1) `--skip-tagged` can't see a target/convention mismatch (Opus R128 from a prior default run reads "tagged" under `--target-lufs -14`); docstring caveat only. (R2) Easy mode counts a no-op album (rsgain "No files were scanned", rc=0) as scanned (:344-345); detect the phrase or a zero read-back and report it distinctly. (R3) M4A read-back logs `b'-6.66 dB'` (`read_gain_strings`, :135: MP4FreeForm is a bytes subclass); decode to str. (R4) Runs that skip everything or are declined at the prompt write nothing to the log (:311-313, :328-330); write a `RG RUN START`/summary pair regardless. (R5) `w[5]` magic index at :308; unpack the tuple like the neighboring loops. (R6) Log open at :281 is unguarded; validate the `--log` path like `directory` (:239-241). Same for rerate.py:115.

- [x] **L-rerate batch (shipped 2026-07-02, v1.0.2; RR4 had already landed with M10).** (RR1) REMAP comment (:41-44) claims extra entries are "harmless": false; 64 is a fixpoint collision (DeaDBeeF 1★ and foobar 2★ share it, unfixable by byte alone) and DeaDBeeF 3★ (190) has no safe mapping either; rewrite the comment to say the map is deliberately closed and why. (RR2) Docstring says "rewrites the POPM byte in place" but `tags.save(v2_version=3, v1=2)` (:83) re-serializes the whole tag (v2.4→v2.3, mints ID3v1); add a docstring sentence, behavior stays (retag.py parity is cited on :82). (RR3) `os.walk` (:135) doesn't prune hidden dirs, unlike replaygain's walker; prune `.`-prefixed dirs so `.testing/` copies aren't touched. (RR4) No error accounting, always exit 0 (:57-59, :71-74 swallow load failures as "no changes"); fold into M10's error plumbing.

- [x] **L-retag batch (shipped 2026-07-02, v1.1.2; RT4's note had already landed with M17).** (RT1) No `old == new` short-circuit: even a correct genre triggers APEv2 delete + full re-save (v2.4→v2.3, ID3v1 minted) on every direct invocation; add a per-file no-op guard (read current genres first; skip write when equal) so direct use is byte-idempotent; genre_tidy already gates via compliance, so this is direct-use hardening. (RT2) `mutagen.File()` returning None for a corrupt Vorbis-family file returns False *silently* (:76-78); print the `[!] Failed` line like every other failure path. (RT3) WMA dry-run always shows old genre as `[]`: `read_genres` (:38) uses `easy=True` which has no ASF wrapper; read `WM/Genre` explicitly for `.wma`. (RT4) `os.listdir` (:151) is non-recursive so `Album/CD1` at the album root touches nothing; acceptable per the "one album directory" docstring, but print the existing "No valid audio files found" note *and* mention subdirectories were not descended. (RT5) Wrong comment at :79: mutagen's `VCommentDict` lowercases keys, so `pop("GENRE")` is dead; drop the second pop and fix the comment.

- [x] **L-genre_tidy batch (shipped 2026-07-02, v1.2.3).** (GT1) No `--layout` flag: `scan_album_dirs` (:214) pins `{artist}/{album}`, so on the genre-foldered library genre_foldermap produces, untagged files fall back to artist=genre-folder; add `--layout` passed through to the scanner exactly like cleaner.py grew it. (GT2) Tabs/newlines in tags corrupt the TSV (verified: genre `"Rock\tPop"` becomes two allowed genres and a fresh map is *not* a no-op, apply rewrites the tag); sanitize `\t`/`\n` → space in `build_rows` fields. (GT3) `parse_map` last-wins silently on norm-key collisions (:119, hand-edited `Jay-Z` vs `Jay‐Z` rows); warn to stderr when overwriting an existing key. (GT4) A real artist named "Various" is permanently unenforceable (`EXCLUDED_ARTISTS`, :94, hard-skip at :315); add one sentence to the EXCLUDED comment block acknowledging the trade-off.

- [x] **L-genre_foldermap batch (shipped 2026-07-02, v1.3.3; GF5 taken as the docs option — RMDIR manifest rows remain unbuilt by choice; GF6 documented rather than re-derived from disk).** (GF1) Docs promise `--revert MANIFEST` undoes a run, but it's a dry run without `--apply` (:12, :49, :63, :584 vs main:591); state "add --apply to execute" in the docstring Usage/Safety blocks and the `--revert` help string. (GF2) No same-filesystem guard: `shutil.move` (:367, :456) silently copy+deletes across devices, against the "mv-only" contract; `os.stat(src).st_dev == os.stat(dst_parent).st_dev` check, refuse with a clear error otherwise. (GF3) Tab/newline in folder names corrupts the manifest TSV (:370, :428-430); refuse to move such paths (issue) or escape on write + unescape on read. (GF4) Genre gate compares case-sensitively (:230): tag `hip hop` vs folder `Hip Hop` is UNKNOWN GENRE, and `--allow-new-genre` then mints a case-variant duplicate top-level folder; match via casefold against the vocabulary and reuse the existing folder's spelling for the dst. (GF5) Dirs removed by `prune_empty` aren't in the manifest so revert can't restore empty structure (:373-391); either record `RMDIR` rows (replayed as mkdir on revert) or soften the "reversible" claim at :49 to files-and-moved-dirs. (GF6) Vocabulary derives from scan records, not disk (:196), so a genre folder with temporarily no readable audio drops out of the gate; either derive from `root.iterdir()` depth-3 structure or document the record-based derivation where the docstring (:37-38) currently claims folder-based. (GF7) Comment fixes: `Move` namedtuple comment (:88-90) claims `kind` affects pruning (it doesn't; only the emit string and stats key); the `dst == path` check (:240-241) is only reachable in the staging-name-as-genre corner, comment it as such or drop it; the "Prune after every move" comment (:415-416) misdescribes a single post-hoc prune and omits the deepest-first sort rationale (:417) that actually matters.

- [x] **L-cleaner batch (shipped 2026-07-02, v1.3.3; C1 taken as the head/tail-sample option, so same-size different-bytes audio now survives as `.from-fragment` instead of being dropped).** (C1) "DROP DUPE (identical size)" (:399-403) is size-only identity; add a cheap first/last-64KiB compare before unlinking, or an explicit caveat in README/docstring that "identical" means byte-count. (C2) MP3s whose tags live only in APEv2/ID3v1 (`ID3NoHeaderError`, :621-624) skip with no log/stat while the contract is "reported and skipped" (the `.wav` path at :772-774 is the model); log and count them. (C3) ASF branch (:683-695) reads keys case-insensitively but writes canonical case without deleting a case-variant original, unlike the Vorbis branch (:657-658); delete variants first. (C4) `_normalize_folder_name`/`_normalize_file_name` (:464-491, :529-549) are near-duplicates; extract a shared `_rename_to()` helper (H3's fix currently has to be made twice, which is the drift risk in action). (C5) `file_count` runs a full `rglob` walk twice per folder (:495-501, sort key + logging); compute once into a dict. (C6) `sum(1 for _ in p.rglob("*") if _.is_file())` (:379) uses `_` as a load-bearing variable; rename to `f`. (C7) Pass-header `run.log("\n--- PASS ---")` calls (:311-314) orphan the timestamp prefix onto its own line; strip leading newlines in `log()` and emit the blank line separately. (C8) The artist survivor is double-recorded as a tag target (:525-526 + :489-491); harmless dedupe, but remove one path and fix the :489 comment (see M20).

### TUI parity gaps and UX improvements

- [x] **(shipped 2026-07-02, v4.9.0) T1 (parity, medium): multi-root config invisible to the TUI, and the TUI can clobber it.** `interactive_menu` (tui.py:801) reads only `get_library_root()`; the CLI honors `get_library_roots()` (cli.py:233-237, config.py:85-95). A `library_roots`-only config makes the TUI treat the install as first-run, prompt, and write a single `library_root` over the user's intent. **Fix:** (1) first-run check consults `get_library_roots()` too (config is "set up" if either key yields a usable root); (2) when multiple roots are configured, show them in the menu header and pass the full list to modes exactly as `cli.py` builds its roots argument; (3) "Change library root" edits `library_root` without touching `library_roots`, and says so on screen. **Test:** monkeypatched config with only `library_roots` set: menu opens without prompting and mode kwargs carry all roots.
- [x] **(shipped 2026-07-02, v4.9.0) T2 (UX, the top item): Esc in a prompt chain accepts defaults instead of cancelling.** In menus Esc means back; in `_tui_prompt_str` Esc means "accept default" (tui.py:337-338, hint "Esc Default"). Selecting a scan by accident and mashing Esc *launches* it with all defaults. **Fix:** make Esc a cancel sentinel: `_tui_prompt_str` returns None on Esc; `_prompt_str` propagates it; every prompt chain in the mode handlers aborts back to the menu on None (a small `_prompt_or_cancel` wrapper keeps the call sites clean); bare Enter keeps meaning "accept default"; update the hint bar to "Enter Accept · Esc Cancel". This changes existing behavior deliberately; note it in patchnotes.
- [x] **(shipped 2026-07-02, v4.9.0) T3 (UX/bug, medium): "Change library root" persists unvalidated paths and destroys the old root.** tui.py:819-821 saves whatever was typed; a typo overwrites the good root, and the recovery loop mislabels itself "First run:" (tui.py:803) while writing every subsequent bad guess to disk. Also: Esc/bare-Enter at the true first-run prompt accepts the default `"."`, silently making the launch CWD the persistent root. **Fix:** validate before persisting (`os.path.isdir`); on failure show "not a directory" and re-prompt with the *old* root intact; only `set_library_root` on success. Give the recovery prompt its own wording ("Configured root missing: <path>"). First-run: require a non-empty explicit path (no `.` default) or confirm the CWD explicitly.
- [x] **(shipped 2026-07-02, v4.9.0) T4 (UX/bug, medium): output-file prompts never expand `~`.** All output prompts are plain `_prompt_str` (tui.py:707, :726, :741, :777, :841, :857, ...) and modes only `abspath`, so `~/reports/x.txt` creates a literal `./~/reports/`. The CLI never hits this (shell expands first). **Fix:** run `os.path.expanduser` on every output path collected by the TUI (one wrapper, e.g. `_prompt_out()`, used at all output prompt sites; do *not* abspath so relative paths keep their current meaning). Show the resolved absolute directory in the pager/pause message afterward ("Report written to /home/.../flac_errors.txt") so "where did my report go" answers itself.
- [x] **(shipped 2026-07-02, v4.9.0; (b) taken as the document-the-coupling option — the prompt now says "(verbose report)") T5 (parity, low, batch):** (a) `--ffmpeg` has no TUI prompt: integrity handlers pass `None` positionally (tui.py:869, :889, :909, :929); add an optional "ffmpeg path (blank = auto)" prompt. (b) "Include OK rows? (y/N)" couples `verbose` to `not only_errors` (tui.py:860-872 and clones); either two prompts or document the coupling in the prompt label. (c) Stats has no layout prompt (tui.py:830-837) though `--layout` applies on the CLI; add the same layout prompt its siblings have. (d) `prefer` is unvalidated free text (tui.py:844) where the CLI has `choices`; re-prompt unless the answer is `flac`/`ffmpeg`. (e) Dead wrong fallback `or "{artist}/{album}"` after layout prompts (tui.py:711-713, :729-731, :743-745, :765-767, :783-786): unreachable, and if reached would ignore a configured layout; drop the `or` arm (prompts already default to `get_layout()`); if a guard is wanted, fall back to `config.DEFAULT_LAYOUT`, never a literal.
- [x] **(shipped 2026-07-02, v4.9.0; (b) taken as scroll-follows-selection; (e) shipped the throttle, `n/total` count, and Ctrl-C hint — the current-file line needs per-file plumbing from every mode and was deliberately left out) T6 (UX, low, batch):** (a) `_prompt_int` (tui.py:83-88) silently swallows bad input; echo "invalid, using N". (b) Menus don't scroll or warn on short terminals (`_tui_select` clips blind below the box); show "terminal too small" or scroll. (c) `_tui_page`: no horizontal scroll, lines chopped at `content_w-4` (tui.py:625) with no truncation indicator, and `max_line_len` recomputed over all lines every keypress (tui.py:590); precompute once, add an ellipsis marker, optionally left/right scroll. (d) Prompt editing is append/backspace only (tui.py:333-345); with pre-filled defaults, editing a long path means erasing it all; at minimum support Ctrl-U (clear field); cursor keys are nice-to-have. (e) `_TUIPbar.draw` erases/redraws the whole screen per file with no throttle, no current-file line, no cancel hint (utils.py:275-311); throttle to ~10 Hz, add a second content line (current file, count `n/total`), and a "Ctrl-C cancels" hint (safe once H6 lands). (f) Stale comment `# Quit (was 4, 0)` at tui.py:815; replace the magic result tuples with named constants and both changelog comments go away. (g) Bad smart-playlist rules print one stderr line *per track* (playlists.py:122-124), which the TUI pages as thousands of identical lines; validate the rule once before the walk (mode-level fix, benefits CLI too).
- [x] **(shipped 2026-07-02, v4.10.0) T7 (UX, deferred, decision needed): persistent curses screen.** Every menu, prompt, pause, and pager is its own `curses.wrapper` session, so multi-prompt flows visibly flash to the shell between widgets, and the init/teardown churn is the root enabler of the H6 lifecycle hazards. The fix is architectural (one screen owned for the whole interaction, widgets drawing into it) and touches every widget; worth doing only as its own deliberate pass. Parked here so it isn't re-litigated from scratch. **Done as its own pass: `interactive_menu` owns one screen (`_open_screen`/`_close_screen`), widgets run through `_with_screen`, `_TUIPbar` draws into the published shared screen, and a mid-session curses failure funnels through one `_degrade_to_text`. Verified under a pty: a full menu→prompt→cancel→mode→pager→quit session enters the alternate screen once, where the same flow on v4.9.0 entered it seven times.**

### Test-suite additions that fall out of the audit

- [x] **Dry-vs-apply parity harness.** The recurring failure shape across cleaner and genre_foldermap is preview divergence. Add a reusable assertion helper (run tool dry, snapshot predicted stats/log classes; run apply on a pristine copy; compare) and apply it to: cleaner three-way merge, cleaner survivor rename, cleaner same-render siblings, genre_foldermap revert, replaygain `--skip-tagged`.
- [x] **Coverage for the untested mains.** `replaygain.main`, `rerate.read_remaps` + `main`, `genre_tidy.cmd_build` merge path, and apestrip's execution loop are all currently unexercised; the mediums above each land a test inside them, which closes most of this gap. Also: tests/test_replaygain.py:22 hardcodes a copy of `AUDIO_EXTENSIONS`; import it from `lattice.config` instead so it can't drift.

## Carry-backs from the cquarry review (2026-07-02)

The usual porting direction inverted: cquarry finished porting this audit (its v3.3.2 through v3.5.0), then its adversarially verified code review confirmed four defects in the shared curses skeleton that lattice-music v4.10.0 still carries. The fixes shipped in cquarry (`CalibreQuarry` commit `0304cfa`, `src/cquarry/tui.py`) are the reference implementation this time. Line numbers are as of v4.10.0. All are small; the Ctrl-C item changes an exit code, so note it in the patchnotes when it lands.

- [x] **(shipped 2026-07-03, v4.10.1) `_run_with_capture` appends the "Report written to ..." footer even when the mode raised or was cancelled.** `if footer:` (tui.py:888) runs regardless of `note`, so the pager shows `[Error]` plus a traceback (or `[Cancelled]`) followed by a success footer claiming a file that was never finished. **Fix (cquarry's):** `if footer and not note:`. **Tests:** a raising func with a footer pages without the footer; a successful func still pages it.
- [x] **(shipped 2026-07-03, v4.10.1; SIGINT-at-text-menu → 130 verified end-to-end) Ctrl-C at the text-fallback menu exits 0 while the curses menu exits 130.** `_fallback_input` (tui.py:563) catches `KeyboardInterrupt` and returns None, which the menu loop reads as Quit; the same keystroke at the curses menu propagates and `interactive_menu` returns 130. A wrapper script checking for the documented 130 sees a clean 0. **Fix (cquarry's):** let `KeyboardInterrupt` propagate (the session handler already maps it to 130); keep `EOFError` as a quiet Quit (None) with a tidy `print()`. **Tests:** monkeypatched `input` raising `KeyboardInterrupt` propagates out of `_fallback_input`; raising `EOFError` returns None; end-to-end, SIGINT at the degraded text menu exits 130 (cquarry verified this under a pty).
- [x] **(shipped 2026-07-03, v4.10.1) `_tui_prompt_str`'s curses.error fallback re-implements the text prompt inline.** tui.py:499-506 duplicates `_prompt_str`'s body, and both copies render a None default as `[None]`. After `_degrade_to_text()` has flipped `_USE_CURSES`, the handler can simply `return _prompt_str(label, default)`; while there, render an empty default as `[]` (`display = default if default else ""`). Same shape in `_tui_pause`'s handler (tui.py:552-557): degrade, then delegate to `_pause()`. **Test:** a curses.error prompt fallback goes through `_prompt_str` (monkeypatch it and assert one call).
- [x] **(shipped 2026-07-03, v4.10.1) Every widget `_run` re-calls `_init_tui_colors()` although the session screen already initialized colors.** Four call sites (tui.py:383, :417, :514, :743) re-run color setup on every widget invocation even though `_open_screen` (tui.py:94) did it once. **Fix (cquarry's):** move the one-shot init into `_with_screen`'s wrapper branch (a small `_boot(stdscr)` that calls `_init_tui_colors()` then the widget body) and delete the four per-widget calls. **Test:** none needed beyond the existing suite staying green; behavior is identical.

## Research 2026-09-12: the companion fold + the music-toolset landscape (from REPORT-12-Sept.md)

A read-only research pass (full evidence in REPORT-12-Sept.md) studied
the apestrip/cleaner fold Brandon asked for and the toolset's gaps
against the music-management state of the art. Verdict: the fold is
feasible, mechanical, and worth its L effort; the landscape gaps are
mostly S audit modes. The fold requires the one contract amendment the
house rules anticipate (CLAUDE.md:69 says "without asking" — this is
the asking), and it carries exactly one deliberate break: inside
`lattice`, write becomes opt-in (`--apply`), while the script shims
keep today's apply-by-default so aliases and cron do not change.

### A. The fold (spec.md §5 amendment + README.md:16 + CLAUDE.md:69 ride this section)

- [ ] **`src/lattice/norm.py`**: promote the pure rules engine
  (normalize_name, canonical_render, tag_fold, tag_dedupe,
  is_legal_name, the fold tables, `_FEAT_RE`, canon_track_artist).
  Zero I/O.
- [ ] **`src/lattice/modes/clean.py`**: the `Run` virtual filesystem +
  the four passes as `run_clean(root, *, dry_run, normalize_names,
  normalize_filenames, normalize_tags, layout, log_path, quiet)`;
  dry-run by default, `--apply` opt-in.
- [ ] **`src/lattice/modes/apestrip.py`**: classify/plan/apply/repair
  as `run_apestrip(root, *, dry_run, keep_metadata, repair_malformed,
  log_path, quiet)`.
- [ ] **CLI + TUI wiring**: `lattice --clean [DIR] [...]` and
  `lattice --apestrip [DIR] [...]` (argparse + dispatch + the
  mandatory TUI entries with `ask_yn` confirms).
- [ ] **Compat shims**: scripts/cleaner.py and scripts/apestrip.py
  become argparse-preserving re-export wrappers (the slipcover.py
  pattern); flags, log defaults, idempotency, and non-TTY
  auto-confirm preserved.
- [ ] **Tests retargeted** to the package modules (~135 tests; delete
  the sys.path hack at test_apestrip.py:17).
- [ ] **Contract amendment**: spec.md §5 rewritten to "reads tags;
  writes metadata only via the explicit --clean/--apestrip write
  modes (opt-in, logged, dry-run by default)"; README.md:16 and
  CLAUDE.md:69 name the two exceptions; patchnotes + version bump.
- [ ] **Ship as v5.0.0**: the contract change is a major (the spec
  amendment says so); the PyInstaller binary ships mutation, so the
  TUI menu entry confirms before applying.

### B. The landscape gaps (each an audit mode unless noted)

- [ ] **Content-hash audio duplicate detection**
  (`--auditAudioDupes`): sha256 exact + head/tail sampling; catches
  retagged/renamed dupes the tag-based `--duplicates` misses. M.
- [ ] **Library health score**: aggregate tag completeness, RG
  coverage, art, bitrate, decode errors into a per-album/per-root
  score. S.
- [ ] **Unimported/stray-file audit**: audio at wrong depth,
  non-audio junk, loose tracks, hidden dirs (integrity prunes them
  today; nothing reports them). S.
- [ ] **`--checkPlaylists`**: dead paths, #EXTM3U compliance,
  relative-vs-absolute in generated m3us. S.
- [ ] **Album-consistency audit**: mixed codec within an album,
  missing year/label, tracknumber gaps. S.
- [ ] **`--auditMbids` + the Picard hand-off TSV**: MusicBrainz TXXX
  coverage from `--auditTags`. S.
- [ ] **ReplayGain verification**: stored gain vs a fresh rsgain/
  ffmpeg ebur128 measurement, read-only. M.
- [ ] **`--auditJunkFrames`**: iTunNORM/private-TXXX reporting. S.
  (The strip itself is a new-companion or retag.py extension, M.)
- [ ] **Art mismatch audit**: embedded vs folder art, per-track art
  divergence. S.
- [ ] **`--snapshot` / `--diff`**: tag+path fingerprint change
  detection between curation runs (no DB by design). S.

Deliberately skipped (already covered or anti-contract): beets-style
import/DB (filesystem is the truth), convert (flac2opus), edit
(retag/genre_tidy), fetchart (slipcover --fetch), lyrics (new-companion
candidate, L, behind 1-7).
